# Disposition Log（判断型决定记录）

规则：每条 J 类规则与每条抑制（pragma / editorconfig 条目）在此登记：站点或范围 → 决定（FIX / SUPPRESS-LOCAL / SUPPRESS-GLOBAL）→ 理由。
决策权：实现子代理自行决策（用户指示 2026-09-19）；任何抑制必须注明明确理由；检查阶段逐条复核；最终独立审计。

| 规则 | 站点/范围 | 决定 | 理由 |
|---|---|---|---|
| MA0076 | 79 处插值包装（benchmarks 21 / src 37 / tests 21 文件分布见 batch-log B4a；覆盖 115 条诊断 = src 57 / tests 22 / bench 36） | FIX | 修复器统一转为 `string.Create(CultureInfo.InvariantCulture, $"…")`：全部站点为日志 / 异常消息 / 诊断路径 / 线程名 / 测试失败消息，输出确定性（不随 CurrentCulture 变化）是本项目想要的改进；`string.Create(provider, ref handler)` 走 DefaultInterpolatedStringHandler，格式直接写入缓冲、无中间字符串分配，分配中性；无 hot-path 站点（logging 属 spec hot-path.md 的 cold edge）；无刻意本地化输出站点。 |
| MA0076 | tests 断言消息 / 期望字符串 21 处 | FIX | 逐 hunk 审查：全部为 `Assert.True`/`Assert.Contains` 的失败消息或测试侧构造的期望事件字符串（生产/假件两侧同步 Invariant，比较一致）；断言条件本身零变更；en_US 环境无观察差异；725 全绿兜底。无站点依赖"当前区域性"才能通过，故无回退/抑制。 |
| MA0076 | src/WinForward.Runtime/RuntimeLogging.cs（EndPoint 格式化两分支） | FIX+手工归一 | 修复器在第一个分支的替换处留下 trivia 残留（`)` 被推到独立行）；手工并回单行（纯空白调整），语义零变更；同步验证 whitespace 子系统不产生新诊断。 |
| MA0076 | tests/WinForward.Core.Tests/NdisApiBatchedSendAbiTests.cs（baseline 站点） | 无需处置（B2 已消除） | `worstChunkBytes` 已由 B2 的 RCS1118 改为 `const`；分析器对已知常量值不报 MA0076（culture-invariant known value），故 B4a 起点为 115 条而非 116 条。 |
| MA0001 | benchmarks/WinForward.Benchmarks/Stability/SoakOptions.cs `entry.IndexOf('=')` | FIX | 加 `StringComparison.Ordinal`。MA0001 只在"默认比较即 Ordinal"且存在显式重载时报（`IndexOf(char)` 默认 ordinal）；显式 Ordinal 语义与原行为完全一致，无分配、无区域性差异。 |
| MA0001 | src/WinForward.Core/IPPrefix.cs `value.IndexOf('/')` | FIX | 同上（char 重载默认 Ordinal → 显式 Ordinal 零行为变更）。 |
| MA0001 | src/WinForward.Runtime/TcpRedirect/ClientResetInjector.cs `session.Server.Host.Contains(':')` | FIX | 同上（`Contains(char)` 默认 Ordinal → 显式 Ordinal 零行为变更）。 |
| MA0159 | src/WinForward.Runtime/RuntimeCounters.cs `GetRegisteredPools`：`OrderBy(static name => name, StringComparer.Ordinal)` → `Order(StringComparer.Ordinal)` | FIX | .NET 7+ 的 `Enumerable.Order(comparer)` 直接按元素排序（identity key 内联），与 `OrderBy(x=>x, comparer)` 相同 comparer、相同排序结果；去掉每元素 key-selector 委托跳转。两者均为延迟缓冲排序，collection expression `[.. …]` 立即枚举，惰性/缓冲差异无观察语义差异；冷路径（诊断快照），无行为风险。 |

## B4b（结构/判断类剩余规则，2026-09-19）

| 规则 | 站点/范围 | 决定 | 理由 |
|---|---|---|---|
| CA1512 | 14 处（BoundedSetupQueue/FlowTable/NativeBufferPool×2/NdisPacketBufferPool/SetupExecutor/Socks5AddressCache/Socks5UdpTransport×2/UdpAssociations×2/UdpResponseReinjector/ProcessAttribution） | FIX | 全部替换为 `ArgumentOutOfRangeException.ThrowIfNegativeOrZero/ThrowIfNegative/ThrowIfLessThan`。异常类型与 paramName 不变（CallerArgumentExpression 取实参表达式 = 原 `nameof(参数名)`），无自定义消息站点；仓库已有 ThrowIf* 先例（NdisApiDriver/Socks5ControlConnection/NativeBufferPool）。 |
| RCS1085 | 21 处自动属性（GcSoakScenario 3 / DurableCaptureBundle 2 / FlowTable / MacAddress / NativeBufferPool 3 / NdisPacketBufferPool / CapturePacketProcessor / ClientResetInjector / TcpProxyCoordinator 2 / TcpProxyRelay / TcpRedirectListener / TcpRedirectSessionStore / UdpProxySession 3） | FIX | fixer 应用 + 逐站点审查：无 `ref`/`out`/`Volatile`/`Interlocked` 引用 backing field（grep 实证），无跨实例字段访问冲突（MacAddress.Equals 已改为 `other.IsValid`）；转换仅消除字段，读写语义不变。 |
| IDE0290 | 20 处（src 13 / tests 5 / bench 2） | FIX | fixer 应用 + 逐站点审查：含验证/回退语义的站点（InterceptionHealthMonitor/ConsoleRuntimeLogger/ClientResetInjector/NdisPacketReinjector/UdpAdapterTargetSource 等）由 fixer 保留为字段初始化器（`_x = 参数 ?? 回退`、`_driver = driver ?? throw`），构造语义逐字保持；其余为纯转发的薄封装。与仓库既有 primary ctor 用法（TcpRedirectInjector/TcpRedirectListener/TcpRedirectSession/TcpProxyRelayFactory/AdapterTransientRetryLogGate）一致。 |
| CA1859 | 14 处（SoakRunner.OpenOutput / Program.RunInterceptionAsync / ConfigurationModels.ParseRule/NormalizeSet/ParseNetworks/ParsePorts / LayeredCaptureRunner.ScopeItemsOf / UdpAdapterTargetSource.CopyMap / AdapterIdentity.GetWindowsIpAdapters / AdapterLocalAddressProvider.GetWindowsAdapterAddresses / ProcessAttribution.ReadUdp4/6+ReadTcp4/6） | FIX | 全部为 private static 成员（非 public seam，不暴露可变集合）；返回/参数类型改为实际构造的具体类型，调用方（同文件或同 assembly）逐处核对通过；ProcessAttribution 四个读取器统一为数组（调用点三元表达式两侧同型，仍成立）。 |
| CA1068 | 9 处重排：NdisCapturePump.RunLoop（1 调用点）、Socks5ControlConnection 私有 ctor/ConnectOnceAsync/ResolveAddressesAsync（各 1–2 调用点）、TcpProxyRelay.PumpAsync（2 调用点）、TcpRedirectSession primary ctor（3 调用点）、UdpProxyCoordinator.SendOnReadySessionSpanAsync（1）、UdpProxySessionContext record（2）、UdpSessionSetup.CreateSessionAsync（2） | FIX | 每处 1–3 个调用点，逐处同步更新；CT 移至最后一个必选参数之后（可选参数保持末尾），无默认值/重载语义变化。 |
| CA1068 | 2 处：Socks5ControlConnection.ConnectAsync 注入缝重载（72）、Socks5UdpTransport.CreateAsync（158） | SUPPRESS-LOCAL | ConnectAsync：该重载刻意镜像 public 重载（CT 紧随必选 server 参数）；必需的 resolveAddresses 缝参数不能排在可选参数之后，单独重排会破坏成对重载的镜像并波及 ~10 处 tests/bench 调用点，收益仅风格。CreateAsync：CT 位于"标识参数之后、仅测试使用的缝工厂之前"，生产调用点（createControl: null, socketFactory: null）因此保持可读；把 CT 排到缝参数后会把 token 埋进 null 实参之间。 |
| MA0189 | 11 处（NdisApiAbi TcpAdapterList×5 + IntermediateBuffer×2；IPHelperAbi MibUdp6RowOwnerPid×1 + MibTcp6RowOwnerPid×2 + MibUnicastIpAddressRow×1） | SUPPRESS-LOCAL | 布局/尺寸背书存在（AssertManagedX64Layout 断言 8836/1566 与逐字段 offset；AssertManagedLayout 断言 IPHelper 行尺寸/offset；UnicastAddressInventoryTests 毒化 padding 锁定 80 字节 stride），但转换本身不可取：fixed buffer 是原生 C 声明（ndisapi.h/netioapi.h）的逐字节镜像，解析代码依赖指针运算（ReadAscii、`ReadOnlySpan(row->Address+off)`、测试中的 `row->Address+offset`），InlineArray 会引入 9 个生成缓冲类型并把上述站点改成 fixed/span 戏法，运行时收益为零、风险集中在只有 Windows 硬件冒烟可完整覆盖的互操作路径。保守保留并在各结构处 pragma（5 对，逐条注明）。 |
| MA0041 | 11 处（TcpFragmentHandlingTests 10 + TcpCoordinatorFakes 1） | SUPPRESS-GLOBAL（.editorconfig） | 规则已被其作者弃用（继任者 CA1822 同族已因 fixer 误修全局禁用）；当前命中全部是读取 primary 构造器参数的属性，加 static 必然 CS9105/CS9113（B2 实测）。修复不可行 + 与 CA1822 同族处置叙事一致 → 在 CA1822/RCS1146/RCS1197/MA0038 块中并入 `dotnet_diagnostic.MA0041.severity = none`（中文注释说明）。选规则级而非 11 条局部 pragma：同一弃用规则族、同一根因，逐站点 pragma 会制造 11 处重复噪音且掩盖"规则族不可用"这一系统性事实。 |
| MA0042 | 30 处 FIX：24 处 `Cancel()`→`await CancelAsync()`（tests）、3 处 `using`→`await using`（CTR，同 RCS1261）、1 处 `dispatch.Result`→`await dispatch`（TcpPendingSynSetupTests，任务已在 WaitAsync 后完成）、2 处 `upstream.Dispose()`→`return upstream.DisposeAsync()`（TcpEof/TcpThroughput UpstreamOwner） | FIX | `CancelAsync` 已是 src 生产惯例（12+ 站点，如 TcpRedirectSessionStore/UdpProxyCoordinator）；CTR.DisposeAsync 对 CTRegistration 与 Dispose 语义等同（无回调则同步完成）；UpstreamOwner 为 `Stream` 包装，`Stream.DisposeAsync` 默认即 Dispose，等价。 |
| MA0042 | 3 处 `releaseFirst.Task.GetAwaiter().GetResult()`（NdisAdapterGateMapTests×2、NdisApiAbiTests×1） | SUPPRESS-LOCAL | gate lease 包装 `Monitor`（线程亲和）：测试必须在持锁线程上同步等待以证明 gate 串行化；改 `await` 后 continuation 可能换线程，lease Dispose 的 `Monitor.Exit` 会抛 SynchronizationLockException。pragma 注明。 |
| RCS1261 | 12 处（9 NetworkStream `using`→`await using`：TcpRelayBenchmarks/LoopbackSocks5Tcp+UdpServer/TcpProxyRelay/TcpProxyRelayTests×3/TcpRelayEndResetTests/TcpRelayObservationTests；3 CTR registration：LayeredCaptureRunner/CaptureLifecycleFakes/CaptureRunnerFakes） | FIX | NetworkStream.DisposeAsync 与 Dispose 均关闭底层 socket（ownsSocket: true），无缓冲差异；CTR 同上。 |
| MA0040 | src 4：LayeredCaptureRunner.PeriodicRefreshTick Task.Run 加 token（同时清 CA2016）、SetupExecutor.DrainItem `TrySetCanceled(item.CancellationToken)`、TcpProxyCoordinator.LaunchSetup 同、UdpProxyCoordinator.ScheduleSessionSetup 同（UDP 处 item.CancellationToken 即 _shutdown.Token，行为等同） | FIX | Task.Run 的 token 只影响调度前取消（此处 token 由本次调用新建，无提前取消路径，行为零变更）；TrySetCanceled(token) 让取消任务携带正确的 token（DrainItem 处与既有 278 行 `TrySetCanceled(exception.CancellationToken)` 惯例一致；TCP 处 item.CancellationToken 当前为 default，行为等同，为将来赋值保持一致）。 |
| MA0040 | bench 8：LoopbackSocks5Tcp/UdpServer 的 Task.Run 与两处 `SendToAsync` 加 `_shutdown`（token）；UdpBurstScenario Task.Run 与 Control/Post 窗口 Delay 加 `senderCancellation.Token` | FIX | 服务器 harness 的发送/接收循环本就以 `_shutdown` 为生命周期，提前取消只会让关停更快；窗口 Delay 归属 sender 阶段（取消发生在窗口之后），行为不变。 |
| MA0040 | bench：UdpBurstScenario `Task.Delay(DrainTime)` | FIX（显式 None） | 该 drain 窗口在 `senderCancellation.CancelAsync()` 之后，传 sender token 会立即抛出；显式 `CancellationToken.None` 表达"有意不传播"（CA2016 同款语义），行为不变。 |
| IDE0059 | 6 处（InterceptionHealthMonitor fired/handler/consecutive；Socks5ControlConnection.ConnectOnceAsync socket；UdpProxyCoordinator.Send readySlot/readySession） | FIX | 删除死初始化（分析器 data-flow 已证明所有可达路径先赋值：InterceptionHealthMonitor 的早期 return 路径不读；socket/ready* 在 lock 内条件赋值 + else return）。`degradedNow = false` 保留（确有必要）。 |
| RCS1229 | 4 处（Socks5UdpTransport.SendSpanAsync；TcpProxyCoordinator.ReinjectExistingFlowDataAsync/HandleReverseAsync/InjectReverseFrameAsync） | SUPPRESS-LOCAL | hot-path.md 约定 #3：steady-state 逐包路径不得引入 async 状态机（~193 B/op），这 4 处刻意以非 async 入口直接返回 ValueTask（注释与 spec 双重背书）；改 async/await 违反性能合同。pragma 注明。 |
| RCS1163 | 3 处 tests：NativeBufferPoolTests `async (index, cancellationToken)`、NdisPacketBufferPoolTests `async (_, cancellationToken)`、NdisCapturePumpTests `(packet, _)` | FIX | 参数改 discard `_`（lambda discard 语义合法），不影响断言。 |
| RCS1163 + IDE0060 | 1 处：TcpProxyCoordinator.HandleFragmentAsync 的 `cancellationToken`（error 级 IDE0060） | SUPPRESS-LOCAL | 参数由 dispatcher 的 fragment-handler 委托签名固定，无法移除；teardown 路径不取 caller token（走 store 的 shutdown token），且对客户端可见的 RST teardown 必须在 caller 取消下完成——刻意未用。pragma 注明。 |
| CA2012 | GcSoakScenario Send（ValueTask 存入 local 后立即 GetAwaiter().GetResult()） | SUPPRESS-LOCAL | 专用 load 线程上的刻意同步消费（既有 S5034/VSTHRD002 pragma 同一叙事），ValueTask 恰好消费一次；随 RCS1222 合并为单行 pragma 列表。 |
| CA2012 | 2 处同步守卫测试（NdisCapturePumpTests 二次 RunAsync、NdisPacketActionExecutorBatchingTests packet.Lease） | SUPPRESS-LOCAL | 守卫在产生 ValueTask 之前同步抛出；Action 绑定 lambda 只断言这一同步异常，不存在 ValueTask 消费。pragma 注明。 |
| RCS1222 | 2 处（GcSoakScenario 相邻 disable/restore） | FIX | 合并为逗号列表（`#pragma warning disable S5034, VSTHRD002, CA2012`），行为不变。 |
| RCS1084 | 2 处（UdpBurst/UdpLoss `Logger = productEvents is not null ? productEvents : NullRuntimeLogger.Instance`） | FIX | 改 `(IRuntimeLogger?)productEvents ?? NullRuntimeLogger.Instance`：左操作数显式上转型使 `??` 在目标类型上成立（首次去 cast 版本 CS0019，已修正）；产物与三元式逐字等价（null→NullRuntimeLogger.Instance）。 |
| RCS1021 | 2 处（DurableCaptureBundle/Program 池计账 lambda） | FIX | 单语句块体 → 表达式体（`Action<bool>` 语句表达式丢弃返回值，与原 `_ = …` 语义相同）。 |
| IDE0270 | 1 处（NdisApiDriver.SendPackets 槽位填充） | FIX | `if (buffer is null) throw …` → `?? throw`；异常类型与 paramName（buffers）不变。 |
| RCS1206 | 1 处（NativeBufferPool.Lease.Memory） | FIX | `_manager is null ? default : _manager.Memory` → `_manager?.Memory ?? default`，等价。 |
| RCS1236 | 1 处（LayeredCaptureRunner.TeardownAsync 的 dispose 失败日志分支） | FIX | 改 `catch (Exception exception) when (stopFault is not null)`：stopFault 为 null 时不再捕获（与原 `else throw;` 等价，栈原样传播）；非 null 时记录，语义一致。 |
| CA2208 | 1 处（UdpProxySession ctor 的 `nameof(context.ReceiveWindowPool)`） | SUPPRESS-LOCAL | 与既有 MA0015/S3928 同一刻意 member-path paramName 模式（注释已说明）；将 CA2208 并入同一条 pragma 列表（质量规范对 CapturedFlowPacketGuards 的同类处置先例）。 |
| CA1419 | 1 处（NdisApiSafeHandle 私有 ctor） | SUPPRESS-LOCAL | 句柄只能经驱动 Open 路径（FromRawHandle）产生；与类型同可见性的构造器会让调用方造出未打开的句柄。规则防护的 marshalling 场景（SafeHandle 作为 P/Invoke 返回类型）在本仓库不存在（原生面用 nint 裸句柄）。 |
| MA0182 | 1 处（AdapterTransientRetryLogGate） | SUPPRESS-LOCAL | 实证并非死代码：WinForward.Cli 经 InternalsVisibleTo("WinForward") 在 Program.cs:276 实例化并接入泵重试日志，测试 AdapterTransientRetryLogGateTests 直接构造；分析器只看本 assembly 使用、看不到 IVT 消费方 → 误报。pragma 注明。 |
| VSTHRD104 | 1 处（AdapterListWatcherTests.FakeTriggerWakesExactlyOneParkedWaiter `parked.Wait(ParkMs)`） | FIX | 该断言探测"被唤醒的另一方在 ParkMs 内仍未完成"；测试改 async，用 `await Task.Delay(ParkMs); Assert.False(parked.IsCompleted);` 表达同一断言（其余 `.Wait` 探测未触发规则，保持原样）。 |
| MA0166 | 1 处（UdpSetupQueueTests 真实时间等待） | FIX | `WaitAsync(TimeSpan, TimeProvider.System, CancellationToken.None)`：该等待是测试同步安全网，必须用真实时间；显式 System 既满足规则又明确与作用域内假时钟（`time`）无关。 |
| CA2016 | 1 处（LayeredCaptureRunner.PeriodicRefreshTick 的 Task.Run） | FIX | 传 `monitorCancellation.Token`（由 `cancellationToken` 链接而来），与 MA0040 同处一并修复。 |

## B5（N 命名，2026-09-19）

| 规则 | 站点/范围 | 决定 | 理由 |
|---|---|---|---|
| IDE1006 | 命名配置重配（`.editorconfig` 6 处 + 约定注释） | FIX-CONFIG | 用户裁定（2026-09-19）：public/protected（含 protected internal）字段 → PascalCase 无前缀（公开字段因性能允许保留）；internal 与 private 同级（static → s_camelCase、实例 → _camelCase）；private/internal const → PascalCase；局部 const → camelCase；按**声明**可见性匹配。实现：non_private_static/readonly 的 `applicable_accessibilities` 收敛为 public/protected/protected_internal；static_fields/instance_fields 显式限定 internal/private_protected/private；constants 去掉 `local`（交还 locals 规则）。 |
| IDE1006 | t_ 机制：临时平行规则 `thread_static_fields`（field+static+同可见性，前缀 t_，声明于 s_ 规则之前，最优语义位置） | SUPPRESS→移除（规则级） + 单站点 pragma | 实证（全量定向 verify，报告存证）：`t_recycleCache` 仍被 s_ 规则裁决（`Missing prefix: 's_'`），全仓 0 条 `missing t_`——多规则"任一满足"语义不成立（单一裁决规则），同域平行规则无法表达"s_ 或 t_"；反向声明会令全部 s_ 静态字段改指 t_（拒绝）。`[ThreadStatic]` 是特性而非修饰符，命名规则无法匹配。→ 移除试探规则；`src/WinForward.Core/PacketRuntime.cs` 的 `[ThreadStatic] t_recycleCache` 单站点 `#pragma warning disable IDE1006`（成对 restore，注释理由：t_ 为 [ThreadStatic] 团队约定）。 |
| IDE1006 | 105 处改名：A1 66（private static → s_）、A2 9（internal static → s_）、A3 12（internal 实例 → _）、局部 const 17（→ camelCase）、方法 `record`→`Record` | FIX | 按用户约定改名并同步全部引用（含 tests/bench 经 IVT 的引用与 XML doc cref）。机制说明：`dotnet format` 无法应用 IDE1006——`NamingStyleCodeFixProvider` 不支持 Fix All in Solution（solution/project/--include 三种范围实测均拒绝、零改动），改用逐行审阅的替换脚本（365 处替换计划全量人工审阅；105 个诊断站点逐一复核改名落地；零改名区实证未误伤；Release build/725 测试/定向 verify exit 0 兜底）。 |
| IDE1006 | 局部 const 17 处由新配置暴露（原被 constants 规则 Pascal 满足而"免检"） | FIX | constants 去掉 `local` 后局部 const 归 locals 规则（camelCase）——正是用户约定；改名语义零风险（编译期常量，无运行时痕迹）。 |
| IDE1006 | A2/A3 中 `Tcp`/`Udp`（internal readonly 实例）等原被 non_private_readonly 满足 | FIX | non_private_readonly 收敛后按约定落入 instance_fields → `_camelCase`（`_tcp`/`_udp`）；引用同步含 `item.Tcp`/`item.Udp` 链式访问。 |
| IDE1006 | `IPHelperId` 局部 const 命名 | FIX（裁定拼写） | 采用 `ipHelperId`（标准 camelCase 缩写处理），同为合规写法且可读性优于逐字小写首字母的 `iPHelperId`。 |

